import React from 'react'
import Child from './Child'

export const Parent = ({name,age}) => {
    return (
        <div>
            {/* <h3>Helloo From {name}</h3>
            <h3>Age {age}</h3>
            <Child name={name}/> */}
            <button  >parent</button>
            <button  >parent</button>
            <button  >parent</button>
        </div>
    )
}
